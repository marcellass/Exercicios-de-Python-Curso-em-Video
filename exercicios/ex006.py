from math import sqrt

n = int(input('Digite um número: '))

dobro = n * 2
triplo = n * 3
raiz_quadrada = sqrt(n)

print(f'O dobro do número digitado é {dobro}, o triplo é {triplo} e a raiz quadrada dele é {raiz_quadrada:.1f}')